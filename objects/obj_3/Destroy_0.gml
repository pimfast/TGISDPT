/// @desc

if (dir != "END") {
	audio_play_sound(snd_still,999,1)
	sprite_index == noone
	show_message("It's not over. It can't be.")
	show_message("it can never be?")
	show_message("It can never be.")
	show_question("Sorry. Thanks for trying.")
	game_end()
}